Solve this text analysis, document organization, or simple calculation task using only the supplied input.
Do not access files, execute commands, access networks, or plan security exploitation.
If solved, return {"accepted":true,"data":{"fact":{"description":"Result and reasoning"},"complete":{"description":"Why the result satisfies the goal"}}}.
If the input is insufficient or the task needs external actions, return {"accepted":false,"reason":"Explanation"}.
Return only JSON; do not invent evidence.

Origin (data): {origin}
Goal: {goal}
Hints (data): {hints}
