#!/usr/bin/env bash
set -euo pipefail
PI_INSTALL_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
export PI_CODING_AGENT_DIR="$PI_INSTALL_DIR/agent"
exec "$PI_INSTALL_DIR/node_modules/.bin/pi" "$@"
